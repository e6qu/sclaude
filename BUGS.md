# Bug Tracker

## Open Bugs

None.

## Fixed Bugs

Numbering is stable: entries superseded by later fixes or made obsolete by
rewrites are pruned, leaving gaps. Tests and docs reference these numbers.

| # | Bug | Fix |
|---|-----|-----|
| 1 | Temp Dockerfile not cleaned up on build failure | `trap cleanup_temp EXIT` with `TEMP_FILES` array |
| 2 | Two extra containers spawned per run for `id -u`/`id -g` | Use host `$(id -u)` / `$(id -g)` directly |
| 4 | Version hash doesn't include UID/GID | Added `USER_UID`/`USER_GID` to hash input |
| 6 | `-it` flags hardcoded without TTY check | `[ -t 0 ]` check: `-it` if terminal, `-i` otherwise |
| 7 | `/tmp` mounted with `noexec` breaks npm/pip tooling | Removed `noexec` from tmpfs flags |
| 8 | `ulimit nofile=1024` too restrictive for Node.js | Raised to 8192 |
| 10 | Workspace path with colons breaks Docker `-v` syntax | Validate and error early if `pwd` contains `:` |
| 12 | Credential sync has no integrity check | JSON validation via `python3 -m json.tool` inside container |
| 13 | `shasum` not available on Linux | `portable_sha256()`: tries `sha256sum` first, falls back to `shasum` |
| 14 | No credential sync on Linux | Added `~/.claude/.credentials.json` and XDG config paths |
| 15 | `echo -e` not portable | Replaced with `printf` |
| 16 | No libsecret/D-Bus in container for Linux keyring | Added `libsecret-1-0 dbus dbus-x11 gnome-keyring` to Dockerfile |
| 17 | `BASH_SOURCE[0]` undefined in zsh | Changed to `${BASH_SOURCE[0]:-$0}` |
| 18 | `#!/bin/bash` shebang not portable | Changed to `#!/usr/bin/env bash` |
| 20 | `python3` used on HOST for credential validation (not guaranteed on Linux) | Moved JSON validation inside the container |
| 21 | `chown -R` on every run causes slow startup with populated volumes | Conditional: only recurse when ownership is wrong |
| 22 | Two extra containers per run (permissions + credentials) | Combined into a single helper container |
| 23 | `version` command spawns up to two containers for JSON formatting | Single container; format locally only if `python3` available |
| 24 | `timeout` not available on stock macOS (test file) | Portable fallback: `timeout` -> `gtimeout` -> none |
| 25 | `useradd` fails if host UID conflicts with existing container user | Added `-o` flag to allow duplicate UIDs |
| 27 | Helper container errors fully suppressed (`2>/dev/null \|\| true`) hides credential sync failures | Print explicit warning on stderr when helper container fails |
| 28 | T05 Linux test doesn't clean up fake `~/.claude/.credentials.json` on failure | Added trap-based cleanup in test subshell |
| 29 | T15 temp file race: other processes creating `/tmp/tmp.*` between before/after counts | Use a unique marker prefix instead of counting all tmp files |
| 33 | Runtime `sudo apt` support broken by `no-new-privileges` | Removed `no-new-privileges`, kept allowlisted sudo for `apt`/`apt-get`/`dpkg`, added the capabilities needed for package management, documented the tradeoff, and added an E2E package-install test |
| 34 | Management commands build the Docker image before dispatch | Dispatch wrapper commands before `ensure_image`; only build for actual CLI execution, `--build`, and `update` |
| 35 | Dockerfile GID handling still fragile when host GID exists in base image | Resolve existing groups by numeric GID with `getent`, create a group only when needed, and avoid assuming the group is named `agent` |
| 36 | `sclaude-apt` volume misleading and incomplete | Replaced it with shared `sagent-apt-cache` and `sagent-apt-lists` volumes used by the supported package-install path |
| 39 | Wrapper flags parsed too late | Added wrapper parsing before dispatch while preserving native CLI args after the first non-wrapper argument |
| 40 | Codex CLI not supported | Added physical `scodex` script, one shared image with both CLIs, and tool-specific config/auth volumes |
| 41 | Claude and Codex flag semantics collide | Do not translate short flags; pass native args unchanged and document `scodex exec` for non-interactive Codex |
| 42 | Codex inner sandbox needs Docker-aware yolo mapping | `scodex` maps default yolo to `--dangerously-bypass-approvals-and-sandbox`; `--no-yolo` leaves native Codex behavior intact |
| 43 | Docker/devcontainer test commands can hang indefinitely when Docker becomes unresponsive | Added portable per-test timeouts and a bounded Docker readiness check to fail cleanly with captured output |
| 44 | Docker image builds hide useful progress because `docker build -q` only prints the final image ID | Removed quiet builds so `sclaude --build`, `scodex --build`, and `update` show full Docker build output |
| 45 | Scripts assume the `docker` command even when users run Podman directly | Added bounded container-engine detection with `SAGENT_CONTAINER_ENGINE=docker\|podman`, trying Docker first and Podman second when unset |
| 46 | Test timeouts killed only the top-level command and could leave child Docker/devcontainer processes running | Added recursive child-process termination to the E2E and devcontainer test harnesses |
| 48 | `sclaude update` / `scodex update` rebuilds the image without updating the wrapper script itself, so a pre-`sagent-sandbox`-rename install keeps looping on "image not found" because its hash never matches a build that survives | `update` now self-updates both wrapper scripts from the latest release before rebuilding, re-execs with the new wrapper (guarded by `SAGENT_SKIP_SELF_UPDATE`), then verifies the image tag and prints the resulting CLI version |
| 49 | `--no-cache` rebuilds reuse the locally cached base image, so OS-level updates never land | `update` now passes `--pull` alongside `--no-cache` to refresh the base image |
| 50 | Docker build context was the wrapper's install directory, so every image build tarred and sent everything beside the script (all of `/usr/local/bin` for release installs — easily 100MB+ — or a repo checkout including `.git`) to the daemon | Build from a fresh temp directory holding only the generated Dockerfile (and, since #68, the optional CA bundle) |
| 51 | `pip install` (even `--user`) fails inside the sandbox with PEP 668 `externally-managed-environment` on Ubuntu 24.04, making the dedicated `sagent-pip` volume unreachable via pip | Set `PIP_BREAK_SYSTEM_PACKAGES=1` in the image; added E2E test T18b |
| 52 | `update` self-update could clobber a source checkout: a symlinked `/usr/local/bin/sclaude` resolves into the git repo, and the downloaded release asset overwrites the working-tree file | Skip wrapper self-update when the resolved script lives in a git work tree; print `git pull` guidance and continue with the image rebuild |
| 53 | Release-tag fetch logic duplicated between `check_release` and `fetch_latest_release_tag` with divergent timeouts | `check_release` reuses `fetch_latest_release_tag` with an explicit 2s budget for the passive per-run check |
| 54 | Builds silently produce no usable image when Docker's default buildx builder uses the docker-container (or cloud) driver: the result stays in the build cache, then every run reports "image not found" and rebuilds again | Pass `--load` on docker builds so the result is exported to the image store (no-op for the default docker driver); podman needs no flag |
| 55 | T10 hides all failure output: `set -e` exits the test subshell when the captured `update` command fails, before the captured output is echoed, so a failing T10 reports `Output: (empty)` | Capture with `\|\| rc=$?` so the output is always echoed before the test exits |
| 56 | `sudo apt` broken for UID-1000 hosts (the common Linux desktop case): ubuntu:24.04 ships a default `ubuntu` user with UID 1000, sudo resolves the UID to that first passwd entry, and the `agent` NOPASSWD rule never applies (`sudo: a password is required`) | `userdel -r ubuntu` before creating `agent`; found by running the E2E suite inside the UID-1000 devcontainer — macOS (UID 501) and GitHub CI (UID 1001) never hit it |
| 57 | Host bind mounts unreadable on SELinux-enforcing hosts (Fedora/RHEL/CoreOS + podman): the Codex config sync's `~/.codex` mount fails with `Permission denied`, and the workspace mount would be equally unreadable at runtime | Codex config files now stream over stdin via tar (no bind mount, which also removed the colon-path limitation for `CODEX_HOME`); the workspace mount runs with `--security-opt label=disable` (a no-op for Docker and non-SELinux hosts) |
| 58 | A corrupted `~/.cache/sagent/release-check` file bricks every run: non-numeric content makes the interval arithmetic fail under `set -u` (`unbound variable`) before the CLI launches, until the file is manually deleted | Validate the cached timestamp is numeric, treating anything else as 0; added E2E test T25 |
| 59 | T10 silently tested released code instead of the code under test: after any release, every open branch's WRAPPER_VERSION is older than the latest tag, so the test's `update` self-updated its wrapper copy to the released asset and re-exec'd that | T10 pins `SAGENT_SKIP_SELF_UPDATE=1`; the self-update download path is instead verified against the real assets by the release workflow after each upload |
| 60 | Release workflow re-runs were not idempotent: `gh release upload` fails on existing assets and the install instructions get appended to the release body a second time; concurrent runs from quick merges could also race | `--clobber` on upload, a body-content guard before the notes edit, and a workflow-level concurrency group; a new step verifies published assets pass the self-updater's checks plus `bash -n` |
| 61 | E2E tests picked their image with `images \| head -1`, but the listing is not newest-first under podman's compat API — with multiple `sagent-sandbox` images present, tests could silently run against a stale image | Tests derive the image tag from the wrapper's own `version` output and verify it exists with `image inspect` |
| 62 | Runtime limits (`MEMORY_LIMIT`, `CPU_LIMIT`, `PIDS_LIMIT`) were baked into the image version hash even though they are container run flags, so tuning any of them forced a pointless full image rebuild | The hash now covers image content only (Dockerfile, UID/GID build args and, since #68, the CA bundle); limits are shown in `version` output instead |
| 63 | A config file with a bash syntax error aborted the wrapper mid-source with an error that never named the file | Validate with `bash -n` before sourcing and fail with a message naming the config file; covered by T28 |
| 64 | `reset` silently reported success while volumes survived: stale never-started/exited sandbox containers (e.g. `--rm` containers that failed to start) pin the volumes, and the `volume rm` failure was swallowed | `reset` first removes non-running sandbox-image containers, then removes volumes individually and fails loudly naming any volume still pinned by a running container; T09b covers the loud path |
| 65 | Running from a directory under `/tmp` gave the agent an empty workspace: the sandbox's tmpfs at `/tmp` shadows the workspace bind mount beneath it (mount ordering is engine-dependent; podman mounts the tmpfs last) | Omit the tmpfs when the workspace is `/tmp` or under it; T12b covers both the mount behavior and the wrapper path |
| 66 | Running from `/` bound the entire host filesystem over the container root — silently defeating the sandbox (the engine accepts `-v /:/`) | `validate_workspace` refuses `/` as the workspace with a clear error; T12c covers it |
| 67 | Back-to-back merges shipped mis-stamped release assets: the workflow run that creates the release can be the one triggered by an earlier commit, and upload-assets built from `github.sha` — a checkout predating the version-bump commit (v2.6.3 shipped wrappers stamped 2.6.2; the verify step failed that run as designed, but the release commit's own run then saw the release existing and reported success) | upload-assets checks out the tag; v2.6.3's assets were re-uploaded from the tag and re-verified |
| 68 | First build fails on corporate networks (seen on a fresh macOS + Rancher Desktop install): a TLS-inspecting proxy re-signs HTTPS with a CA the host trusts but the fresh Ubuntu build container does not, so the `gh` keyring download dies with `curl: (60) SSL certificate problem` (the nodesource and `npm install` steps would fail the same way, and so would every HTTPS request from inside the sandbox). Docker Desktop and Rancher Desktop apply host CAs to image pulls only | `SAGENT_CA_BUNDLE` (env or config file) names a PEM bundle that is validated, split one-file-per-certificate into the build context, installed via `update-ca-certificates` before the first HTTPS fetch, and exported for the tools with private stores (`NODE_EXTRA_CA_CERTS`, `SSL_CERT_FILE`, `REQUESTS_CA_BUNDLE`, `PIP_CERT`); the bundle content is part of the image hash; a failed build now prints the diagnosis and the macOS/Linux export commands instead of exiting silently; T31/T32 |
| 69 | `scodex` forwarded the host's `SSL_CERT_FILE` and `CODEX_CA_CERTIFICATE` into the sandbox, where those host paths do not exist, so on exactly the networks that set them Codex lost its trust store entirely | Removed the passthrough; extra CAs go through `SAGENT_CA_BUNDLE`, which sets `SSL_CERT_FILE` inside the image (Codex falls back to it when `CODEX_CA_CERTIFICATE` is unset) |
| 70 | Engine selection failed with a generic "neither docker nor podman is available" even when a docker CLI was installed but its desktop app/VM was still starting, or when Rancher Desktop was in containerd mode (only `nerdctl`/`rdctl` on PATH) | The error names the situation found on PATH: an installed CLI whose daemon did not answer (start Docker Desktop/Rancher Desktop/colima/podman machine), or Rancher Desktop without a docker CLI (switch to the dockerd engine); `verify_image_built` and T19 also check `gh` |
| 71 | A workspace behind a symlink mounted empty on VM-backed engines that share only physical paths: on macOS `/tmp` is a symlink to `/private/tmp`, podman machine shares `/private` but the client passes the logical path through, so `sclaude` run from `/tmp/x` gave the agent an empty directory (Rancher Desktop and Docker Desktop resolve it themselves) | `validate_workspace` mounts the physical path (`pwd -P`) at the logical path (`pwd`), so the engine gets a path it can resolve while the in-container path (and the tools' per-directory session keys) stay unchanged; T12b/T30/T31 mount their fixtures the same way |
| 72 | Most E2E test bodies ran under `bash -c` without errexit, so only the last command's status counted: T12b's mount assertion, T07's persistence check, T19's `claude`/`codex` probes, T20's `auth.json` check and T28's first two assertions were all vacuous (T12b "passed" on a host where the mount was actually empty, see #71; T29's URL-count assertion could never have passed because both copies of the URL sit on one line) | Every test body runs under `bash -ec`; T17b/T17c capture their command status with `\|\| rc=$?` so diagnostics still print |
| 73 | Two more tests asserted nothing on macOS: T15 searched `/tmp` for leaked temp files while `mktemp` creates them under `$TMPDIR` (`/var/folders/...`), and T12 never mounted its spaced path (it only ran `version` from it) | T15 searches `${TMPDIR:-/tmp}`; T12 bind-mounts the spaced directory and reads a file through it |
| 74 | On Rancher Desktop and colima a workspace outside the home directory silently mounted as an empty directory (their VMs share only `$HOME` plus `/tmp/rancher-desktop` or `/tmp/colima`), so the agent saw no files and no error; the macOS CI job had been "passing" that way | `validate_workspace` recognizes both by their docker contexts (`rancher-desktop`, `colima`, `colima-<profile>`) and refuses such a workspace with a message naming the shared paths; `SAGENT_SKIP_SHARE_CHECK=1` covers setups with extra shares; the macOS jobs keep bind-mounted fixtures under `$HOME`; T33 |
| 75 | On a rootless engine the workspace was unusable inside the sandbox: rootless podman/docker map the container's non-root UIDs to subordinate UIDs, so the sandbox user (host UID in the image) could not read a `0700` workspace and could not write a `0755` one. Never caught because the tests that mount host directories asserted nothing (#72) | The podman CLI gets `--userns=keep-id:uid=<host>,gid=<host>` on every container the wrapper starts (helper and tool) when the server is rootless; the docker CLI cannot request that mapping, so a rootless daemon behind it is refused with guidance before any engine call; the compat-socket CI job now serves a rootful socket; T12b proves read and write, T34 covers the refusal |
| 76 | A toolchain change orphaned or corrupted caches silently: pip site-packages are per Python minor, npm native addons per Node ABI, apt and nested-podman state per Ubuntu release, so after a base-image or version bump the agent found missing packages and stale caches with no explanation, and the fix was a manual `reset` | Cache volumes carry a `.sagent-stamp` of the toolchain they were filled for; the pre-run helper clears a volume whose stamp differs (or is missing) and says so, so upgrades need nothing from the user; `volumes` reports sizes per volume and the caches total, and `reset-caches` clears caches on demand; T36/T37 |
| 77 | On networks that intercept TLS the first build still failed 20 minutes in, and the fix was a manual keychain export the user had to get right (System keychain only; the CA can live in the login keychain) | Every build is preceded by a probe that runs a build step's fetch in the plain base image; on TLS failure the wrapper exports the host trust store (macOS System and login keychains, Linux system bundle), verifies it fixes the fetch, saves the bundle and writes `SAGENT_CA_BUNDLE` to the config file, and builds; a host that lacks the CA, or a configured bundle that lacks it, stops before the build naming the chain issuer; `doctor` runs the same probe; T41 |
| 78 | Signing in from inside the sandbox failed: the browser-open shim rendered Claude Code's localhost-callback URL as the link, and the browser's callback to `localhost:<random port>` can never reach the listener inside the sandbox; `codex login` likewise waited on `127.0.0.1:1455` inside the container | The shim rewrites Claude Code's sign-in URL to the manual-code redirect it prints itself as the fallback (the code page appears, the code is pasted at the CLI's prompt, and the token exchange uses that redirect whenever the local listener saw nothing); `scodex login` adds `--device-auth`, the mode OpenAI documents for containers, unless another mode was asked for; T29/T42 |

| 79 | Commits made inside the sandbox had no author when the identity came from the repository itself or a conditional include: the sync read `user.name`/`user.email` from the global config alone, and `doctor` then reported "no identity in the host's global git config" on machines that commit fine | Both read the identity git resolves in the workspace, and the sync writes it into the synced config when the global config has none; a repo-local value still wins inside the sandbox; T20c |
| 80 | Every run printed a "time stamp is in the future" warning per synced file when the host clock ran slightly ahead of the engine's VM | The host state is extracted with `tar -m`: the staged files are copies remade each run, so their mtimes carry nothing; T20b |
| 81 | A release published nothing when release-please failed after creating it (a transient API error), because every publishing job was gated on that job's output, and re-running skipped them again since the release already existed | The publishing jobs take their tag from a job that reads it either from release-please or from a `workflow_dispatch` input, so an unfinished release can be completed with its tag; each states its own condition, since GitHub carries a skipped ancestor down the whole graph |
| 82 | A refreshed CA bundle never reached the build context: on a network whose proxy CA had changed, the wrapper re-exported the CA and recomputed the image hash, then built from the certificates staged before the refresh and blamed the network when the first fetch failed | The context is re-staged after the probe, unconditionally; T32c |
| 83 | The build-failure guidance stopped at its first line: the disk check inside it is a pipeline, so under `pipefail` a probe that could not start aborted the function, hiding the disk, TLS and retry advice and exiting with the probe's status | The probe's failure is tolerated where it is read; T32d |
| 84 | `uv tool install` and the Pythons uv manages lived in the pip volume, which is cleared whenever the image Python changes, so an upgrade silently deleted tools someone had installed | `~/.local/share` is its own volume, and what is already in the old place moves into it on the next run, before the cache stamps are checked; T37b |
| 85 | Installing and updating needed `sudo`, and every "new release" prompt repeated a `sudo curl` into `/usr/local/bin` | `install` puts both wrappers in `~/.local/bin` (or a directory you name) and adds it to the shell startup file once; `update` moves an install from outside the home directory, needing the password only to clear what sudo left; T44 |
| 86 | `config set` wrote values double-quoted into a file that is sourced, so a path holding `$` or a backtick was corrupted and a command substitution would have run on every start; `tools enable all` failed outright with no JDK, naming Java tools the user never typed | Values are written single-quoted; a group drops the Java tools when there is no JDK, while naming one still errors; T38b |
| 87 | `update` treated every install outside the home directory as one that needed sudo, so a deliberately chosen writable directory (`/opt/bin`, a shared `/srv/tools/bin` on PATH) was moved into `~/.local/bin` and the original emptied — found by installing a release the way a user does | The migration first checks the directory is one this user cannot write; a writable one is left where it is; T44 |
| 88 | `sclaude tools`, `version`, `status`, `config list` and `volumes` died with "write error: Broken pipe" when their output went to a reader that stops early (`\| grep -q`, `\| head`) — the longer tool list made it a common race rather than a rare one | Each builds its output and writes it once, through a helper that ignores a closed pipe; doctor stays progressive on purpose. T38b |
| 89 | Every release PR showed a failed CI check: the release-PR guard filters out all ten jobs, a run with zero jobs has nothing to conclude from, and GitHub calls that a failure ("This run likely failed because of a workflow file issue") | One job that never filters itself out, which states whether the others were skipped on purpose; the run concludes on it |
| 90 | A container engine that fell over mid-suite (the Rancher Desktop VM losing its network) failed tests that had nothing to do with it, and with them the job | A failure whose output says the engine went away is retried once, after waiting for the engine to answer again, and says RETRY where it does so; a test that failed on its own merits is never retried; T48 |
| 91 | Publishing v2.19.0 half-finished: the arm64 image built and passed its checks, then `docker push` came back "unknown blob" from ghcr, so no manifest was published and the release had wrappers and one architecture | The push and the manifest creation are retried up to three times, twenty seconds apart; both are idempotent, and the image grew to 5.5 GB, which is when the registry started doing this |
| 92 | T19c started the host clipboard agent in the background and used it immediately, so on a loaded runner the shim inside the sandbox gave up before a backgrounded bash had reached its serving loop | The test sends one request and waits for its answer before running the sandbox, which is the only proof the agent is up |

## False Positives

| Item | Reason |
|------|--------|
| `date` portability | `date -u +FORMAT` works on both BSD and GNU; flagged as fragility note only |
